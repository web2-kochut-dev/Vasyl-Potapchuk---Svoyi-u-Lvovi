/**
 * Центральна конфігурація інтеграцій (аналітика + доставка заявок).
 *
 * Поки значення порожні — відповідна функція просто вимкнена й нічого не ламає:
 *  • gtmId порожній  → аналітика не вантажиться;
 *  • leadWebhookUrl порожній → заявка пишеться лише в консоль (fallback).
 *
 * Щоб УВІМКНУТИ — впишіть сюди реальні значення (це єдине місце, яке треба
 * змінити) і задеплойте. Жодних інших правок коду не потрібно.
 */
export const SITE_CONFIG = {
  /**
   * Контейнер Google Tag Manager, напр. 'GTM-XXXXXXX'.
   * Усередині GTM налаштовуються GA4 і Meta Pixel — тут потрібен лише ID контейнера.
   */
  gtmId: '',

  /**
   * URL вебхука автоматизації (Google Sheets + Telegram).
   * Фронт шле сюди POST з JSON-заявкою.
   */
  leadWebhookUrl: '',
} as const;
